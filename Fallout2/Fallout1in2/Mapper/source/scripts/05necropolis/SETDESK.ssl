#include "..\headers\define.h"

#define NAME                    SCRIPT_SETDESK

#include "..\headers\command.h"

procedure start;
procedure do_stuff;

procedure start begin
end

procedure use_p_proc begin
	if (local_var(0) == 1) then begin
		display_msg(mstr(100));
	end
	else begin
		display_msg(mstr(101));
		set_local_var(0, 1);
	end
end
