#include "define.h"

procedure start;

procedure start begin
end

procedure spatial_p_proc begin
  if source_is_dude then begin
	 if (local_var(0) != 1) then begin
		display_msg(message_str(SCRIPT_ODDWALL, 100));
		set_local_var(0, 1);
	 end
  end
end
