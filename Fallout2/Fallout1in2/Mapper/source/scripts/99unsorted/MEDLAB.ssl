#include "define.h"

procedure start;

procedure start begin
end

procedure spatial_p_proc begin
  if source_obj == dude_obj then begin
	 if (local_var(0) == 0) then begin
		display_msg(message_str(SCRIPT_MEDLAB, 100));
		set_local_var(0, 1);
	 end
  end
end
