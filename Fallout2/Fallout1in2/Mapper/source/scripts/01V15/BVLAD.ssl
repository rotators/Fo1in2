#include "..\headers\define.h"

// TODO: Script unused. Can be deleted.

procedure start;


procedure start begin
end

procedure use_p_proc begin
   /*if ((anim_busy(dude_obj) != 0) or (anim_busy(dude_obj) == 0)) then begin
      script_overrides;
      load_map("VaultEnt.map", 1);
   end*/
end

