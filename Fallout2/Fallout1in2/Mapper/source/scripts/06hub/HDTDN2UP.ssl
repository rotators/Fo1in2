/*

   Hub - Stairs from Decker's hideout to Maltese Falcon

*/

/* Include Files */
#include "define.h"
#include "maps/hubdwntn.h"

#define NAME                    SCRIPT_HDTDN2UP

#include "command.h"

/* Standard Script Procedures */
procedure start;
procedure use_p_proc;

procedure start begin
end

procedure use_p_proc begin
   if (map_var(MVAR_COPS_IN_HIDEOUT) == 0) then begin
      if ((anim_busy(dude_obj) != 0) or (anim_busy(dude_obj) == 0)) then begin
         script_overrides;
         move_to(dude_obj, 25539, 0);
      end
   end
   else begin
      display_msg(mstr(100));
   end
end
