#include "..\headers\define.h"

procedure start;
procedure start
begin
	if global_var(DEBUG_MODE_MESSAGES_ON) then begin// if debugmsg enabled, notify script is being run
		debug_msg("GLOW4W~1.SSL");
	end
end