/*

   This script is only used as a "message holder" so critters can float specific text. It is not physically used in the game.

   Example:
   ZACK.ssl
      float_msg( self_obj, message_str(SCRIPT_ENEMY, random(100, 105)), FLOAT_MSG_RED );

*/

#include "..\headers\define.h"

procedure start;
procedure start begin
	if debug_mode then begin
		display_msg("ENEMY.SSL");
	end
end
