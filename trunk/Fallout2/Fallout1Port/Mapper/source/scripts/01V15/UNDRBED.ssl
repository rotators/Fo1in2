#include "..\headers\define.h"
#include "..\headers\command.h"

procedure start;
procedure start
begin
	if debug_mode then begin// if debugmsg enabled, notify script is being run
		display_msg("UNDRBED.SSL on hex: " + self_tile + " elevation: " + self_elevation);
	end
end
