#include "..\headers\define.h"

procedure start;


procedure start
begin
	if ((script_action == 6) and (source_obj == dude_obj)) then begin//use_p_proc - Use, Activate, or Manipulate the Object or Item
		script_overrides;
		load_map("Glowent.map", 2);
	end
end

