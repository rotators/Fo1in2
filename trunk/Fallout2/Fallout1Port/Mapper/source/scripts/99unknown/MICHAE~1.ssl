#include "..\headers\define.h"

procedure start;
procedure start
begin
	if global_var( GVAR_DEBUG_MODE_MESSAGES_ON ) then begin// if debugmsg enabled, notify script is being run
		display_msg("MICHAE~1.SSL");
	end
end
