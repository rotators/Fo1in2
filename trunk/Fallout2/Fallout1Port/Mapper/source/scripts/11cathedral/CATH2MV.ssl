#include "..\headers\define.h"

procedure start;

procedure start
begin
	if (script_action == 6) then begin//use_p_proc - Use, Activate, or Manipulate the Object or Item
		script_overrides;
		load_map(MAP_MSTRLR12, 0);
	end
end
